<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
      xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:background="@color/colorSecondaryDark">

    <TextView
          android:id="@+id/cardCounter"
          android:layout_width="@dimen/size_web_recyclerchip_counter"
          android:layout_height="@dimen/size_web_recyclerchip_counter"
          android:gravity="center"
          app:layout_constraintStart_toStartOf="parent"
          app:layout_constraintTop_toTopOf="parent"
          app:layout_constraintBottom_toBottomOf="parent"
          android:elevation="@dimen/elevation_card"
          android:background="@drawable/button_round"
          android:textColor="@color/colorSecondaryLight"/>

    <TextView
          android:id="@+id/cardName"
          android:layout_width="0dp"
          android:layout_height="wrap_content"
          android:gravity="center"
          app:layout_constraintStart_toEndOf="@id/cardCounter"
          app:layout_constraintEnd_toStartOf="@id/cardBtnExpand"
          app:layout_constraintTop_toTopOf="parent"
          android:elevation="@dimen/elevation_standard_small"
          android:textSize="@dimen/textsize_topic_title"
          android:textColor="@color/colorSecondaryLight"/>

    <Button
          android:id="@+id/cardBtnExpand"
          android:layout_width="@dimen/size_touch_standard_mini"
          android:layout_height="@dimen/size_touch_standard_mini"
          app:layout_constraintEnd_toEndOf="parent"
          app:layout_constraintTop_toTopOf="parent"
          app:layout_constraintBottom_toBottomOf="parent"
          android:drawableStart="@drawable/ic_arrow_down"
          android:background="?attr/selectableItemBackground"/>

</androidx.constraintlayout.widget.ConstraintLayout>